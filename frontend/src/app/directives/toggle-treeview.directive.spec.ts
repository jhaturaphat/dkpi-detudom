import { ToggleTreeviewDirective } from './toggle-treeview.directive';

describe('ToggleTreeviewDirective', () => {
  it('should create an instance', () => {
    const directive = new ToggleTreeviewDirective();
    expect(directive).toBeTruthy();
  });
});
