const express = require('express')
const router = express.Router()
const group = require('../models/indicator.model')

router.route('/indicator?')
.get((req,res, next) => {
   
})

module.exports = router; 